# Block direct web access to shared files — they must be served only
# through the authenticated shared-file-download.php endpoint, which
# checks that the requester is the sender or a listed recipient.
Require all denied
